using UnityEngine;
using UnityEngine.UI;

namespace SGMG.AR_MyPet
{
    public class HappinessBar : MonoBehaviour
    {
        [Header("UI Reference")]
        public Slider happinessSlider;

        // ✨ 나머지 연결 변수(hungerScript, moodScript)와 계산 로직 모두 삭제
    }
}
